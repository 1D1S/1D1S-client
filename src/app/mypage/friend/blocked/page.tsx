import BlockedMembersScreen from '@feature/friend/screen/BlockedMembersScreen';
import React from 'react';

export default function BlockedMembersPage(): React.ReactElement {
  return <BlockedMembersScreen />;
}
